// MARK: 題目１：
var sum = 0
/*
 
 寫一個迴圈 讓下面這行印出 0~100的總和
 
 */
print(sum)

// MARK: 題目２：
var array = [1, 3, 5, 7, 9, 11]
/*
 
 寫一個迴圈 讓下面這行印出各自的平方 [1, 9, 25, 49, 81, 121]
 
 */
print(array)

// MARK: 題目３：
/*
 
 寫出一迴圈程式從1跑到100
 如果該數字被3整除的話 print出 fizz
 如果該數字被5整除的話 print出 buzz
 如果該數字被3而且也被5整除的話 print出 fizzbuzz
 
 */

// MARK: 題目４：
let arr = [-1, 3, 7, 11, 18]
let target = 14
var result: [Int] = []
/*
 
 寫出一個迴圈 從arr裡面找到兩個數字的總和為target的並把它存到result
 
 */
print(result) // [3, 11]
